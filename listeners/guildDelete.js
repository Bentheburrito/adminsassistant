module.exports = (client, guild) => {
    console.log(`The client has been removed from the guild ${guild.name}, or the guild was deleted.`);

    // Same w/ guildCreate, if we are storing guild data then it can be deleted:
    // ~~~
}